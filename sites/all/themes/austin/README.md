Austin Scarlett v2 Custom Theme
---------------------------------
Originally based on the tao theme, but completely overhauled to be cleaner and faster. Utilizes Compass/SASS as the CSS preprocessor.

